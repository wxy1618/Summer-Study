# 第七周笔记：T5、ChatGPT 原理、预训练模型微调与 RAG

## 一、本周学习目标

第七周的主题从前几周的 NLP 基础任务继续向“大语言模型应用与微调”推进。前几周已经学习了词向量、序列模型、注意力机制、翻译、问答、自然语言生成、共指消解等内容，本周重点放在三个层面：

1. 从模型结构层面理解 T5、Transformer 与 ChatGPT 类模型的关系；
2. 从训练范式层面理解预训练、指令微调、偏好对齐和参数高效微调；
3. 从应用工程层面熟悉 HuggingFace 微调流程、离线知识检索增强生成和在线知识微调流程。

本周不以配置环境为主要目标，而是通过代码文件整理可复现实验流程。代码覆盖文本分类、抽取式问答、指令数据构造、LoRA 参数分析、离线 RAG 和在线微调流程记录。

## 二、T5 与统一 Text-to-Text 框架

### 2.1 T5 的核心思想

T5 全称为 Text-to-Text Transfer Transformer，其重要贡献是把各种 NLP 任务统一成“文本输入到文本输出”的形式。例如：

```text
translate English to Chinese: I love machine learning.
-> 我喜欢机器学习。

sst2 sentence: This movie is surprisingly good.
-> positive

question: What is LoRA? context: LoRA freezes base weights ...
-> a parameter-efficient fine-tuning method
```

这种统一框架的好处是任务格式一致，模型接口一致，预训练和下游任务之间的差异被转化为输入提示和目标文本的差异。T5 论文提出的关键点包括：

- 统一任务形式：分类、翻译、摘要、问答都被看作文本生成；
- Encoder-Decoder 架构：编码输入文本，解码目标文本；
- Transfer Learning：先在大规模语料上预训练，再在下游任务上微调；
- 任务前缀：通过自然语言前缀告诉模型当前任务类型；
- C4 语料：使用大规模清洗网页语料进行预训练。

### 2.2 T5 与 GPT 类模型的差异

T5 通常使用 Encoder-Decoder 架构，适合条件生成任务，例如翻译、摘要、问答。GPT 类模型通常使用 Decoder-only 架构，通过自回归方式预测下一个 token，更适合开放式生成、对话和指令遵循。

两者并不是优劣关系，而是建模方式不同：

- T5 更强调“给定输入，生成输出”的任务统一；
- GPT 更强调“给定上下文，继续生成”的自回归语言建模；
- ChatGPT 类模型在 GPT 基础上进一步加入指令微调、偏好学习和安全对齐；
- 在工程应用中，T5 常用于结构化 Seq2Seq 任务，GPT/LLM 常用于对话、RAG、Agent 和复杂推理。

### 2.3 对本周作业的启发

`01_t5_text_to_text_tasks.py` 把文本分类、摘要、问答等样例统一转成 T5 风格的输入输出对，帮助理解“任务前缀 + 文本目标”的数据组织方法。该脚本不依赖外部深度学习环境，重点是让数据格式和任务抽象清晰可见。

## 三、ChatGPT 原理剖析

### 3.1 语言模型的基本目标

ChatGPT 的基础仍然是语言模型。语言模型学习的是给定上下文时下一个 token 的概率：

```text
P(x_t | x_1, x_2, ..., x_{t-1})
```

在 Transformer 中，模型通过自注意力机制读取上下文，形成每个 token 的上下文表示，再通过输出层预测下一个 token。规模扩大后，模型会从大量文本中学习语法、事实片段、任务格式、推理模式和代码结构。

### 3.2 ChatGPT 的训练链路

从公开资料和 InstructGPT 论文可以概括出一个典型训练链路：

1. 预训练：使用大规模文本做自监督学习，使模型获得通用语言建模能力；
2. 监督微调：使用人工编写或筛选的指令-回答样本，让模型学习“按要求回答”；
3. 奖励模型：收集人类对不同回答的排序，训练一个偏好评分模型；
4. 强化学习或偏好优化：让模型输出更符合人类偏好的回答；
5. 安全与质量评测：通过拒答策略、事实性评测、红队测试等降低风险。

### 3.3 为什么指令微调重要

仅靠预训练，模型更像是一个强大的“文本续写器”，不一定稳定遵循用户意图。指令微调把任务写成 `instruction + input -> output`，使模型更明确地学习：

- 用户要求是什么；
- 哪些上下文是可用证据；
- 输出格式应当如何组织；
- 遇到未知信息时如何表达不确定性；
- 在安全或伦理边界上如何处理请求。

这也是本周 `02_instruction_tuning_dataset_builder.py` 的重点：不是直接训练大模型，而是先把高质量指令数据的结构、划分、校验和评估字段整理清楚。

## 四、大模型微调方法

### 4.1 全量微调

全量微调会更新模型全部参数。它表达能力强，适合数据量较大、任务与基座模型差异较明显、资源较充足的场景。但对大模型而言，全量微调存在显著成本：

- 显存占用高；
- 训练速度慢；
- 每个任务保存一份完整模型，存储成本高；
- 小数据场景下更容易过拟合；
- 对训练稳定性和数据质量要求高。

### 4.2 Prompt Tuning

Prompt Tuning 不更新模型主体参数，而是学习一小段连续的软提示向量。它的思想是让模型主体保持不变，只在输入端增加可训练提示。优点是参数量小、存储方便；不足是对模型规模和任务类型比较敏感，某些复杂任务上效果可能不稳定。

### 4.3 P-Tuning / P-Tuning v2

P-Tuning 进一步把连续提示向量做成更灵活的结构。P-Tuning v2 借鉴 Deep Prompt Tuning，把可训练提示插入多层，使其在更多 NLU 任务上接近全量微调表现。它适合希望尽量少改动模型参数、又需要比较强适配能力的场景。

### 4.4 LoRA

LoRA 的核心思想是冻结原始权重矩阵，只训练低秩更新矩阵：

```text
W' = W + BA
```

其中 `W` 是冻结的原始权重，`A` 和 `B` 是可训练的低秩矩阵。若原始矩阵大小为 `d_out * d_in`，LoRA 额外参数约为：

```text
r * (d_in + d_out)
```

当秩 `r` 远小于 `d_in` 和 `d_out` 时，训练参数量会大幅下降。LoRA 的优势包括：

- 显著减少可训练参数；
- 保留基座模型权重，便于多任务共享；
- 适配器体积小，便于保存和部署；
- 训练后可合并到基座权重，推理时不一定增加额外延迟。

`03_peft_method_parameter_accounting.py` 用可解释的参数量计算展示全量微调、Prompt Tuning、P-Tuning 和 LoRA 的差别。

## 五、LangChain 与 RAG 基础

### 5.1 RAG 的动机

大语言模型的知识来自训练阶段，无法天然知道本地文档、课程资料、最新接口或私有数据库。RAG 的核心思想是：

```text
用户问题 -> 检索相关文档 -> 构造带证据的 prompt -> 模型生成答案
```

这样做有三个目的：

- 把回答锚定到可追溯材料；
- 减少模型凭记忆编造；
- 在不重新训练模型的情况下引入新知识。

### 5.2 RAG 的典型模块

一个基础 RAG 系统通常包括：

1. 文档加载：读取 PDF、网页、Markdown、数据库或知识库；
2. 文档切分：按长度、标题或语义边界拆成 chunk；
3. 向量化：使用 embedding 模型把文本映射到向量；
4. 索引存储：把向量和原文保存到向量数据库或本地索引；
5. 检索召回：根据用户问题找出最相关 chunk；
6. 上下文拼接：把证据放入 prompt；
7. 生成与引用：模型基于证据回答，并保留来源。

LangChain 的价值在于把模型、检索器、向量库、工具调用和链式流程连接起来。实际工程中需要特别注意：chunk 太小会丢失语义，chunk 太大又会降低检索精度；召回质量差时，生成模型再强也容易回答偏题。

### 5.3 本周离线 RAG 实验

`04_offline_rag_pipeline.py` 使用标准库实现一个可解释的离线 RAG 原型。它不调用外部 API，而是完成以下流程：

- 构造课程知识库；
- 按句子窗口切分文档；
- 使用 TF-IDF 进行检索；
- 根据检索证据拼接 prompt；
- 生成一个抽取式答案；
- 保存检索得分、证据片段和回答结果。

该实验重点不是追求生成质量，而是把 RAG 的“索引-检索-证据-回答-评估”链路拆开观察。

## 六、HuggingFace 微调流程

### 6.1 文本分类

文本分类是预训练模型微调中最常见的任务。流程通常为：

1. 准备 `text` 和 `label` 字段；
2. 使用 tokenizer 把文本转成 `input_ids` 和 `attention_mask`；
3. 使用 `AutoModelForSequenceClassification` 加载分类模型；
4. 定义评价指标，如 accuracy、precision、recall、F1；
5. 使用 `Trainer` 或自定义训练循环进行微调；
6. 保存模型、tokenizer 和实验记录。

`05_hf_text_classification_finetune.py` 采用 HuggingFace 风格组织文本分类任务。没有安装依赖时，脚本会输出实验计划；有依赖时，可按参数运行最小样例。

### 6.2 问答任务一：特征构造

抽取式问答的输入是 `question + context`，输出是答案在 context 中的起止位置。难点在于长文本可能超过模型最大长度，因此需要滑动窗口、截断策略和 offset mapping。

`06_hf_question_answering_feature_builder.py` 用标准库模拟问答特征构造，重点展示：

- question/context 拼接；
- 字符级答案位置；
- token offset；
- 长文本窗口；
- start/end label 对齐。

### 6.3 问答任务二：微调流程

`07_hf_question_answering_finetune.py` 对应 HuggingFace 抽取式 QA 微调流程。脚本保留了依赖检查、样例数据、tokenizer 映射、训练参数和预测解码逻辑。它的目的不是替代完整 SQuAD 训练，而是把“从样本到 span logits 再到答案文本”的关键步骤写清楚。

### 6.4 BERT 与 RoBERTa 选做

BERT 和 RoBERTa 都可用于文本分类、问答等任务。BERT 使用 NSP 等预训练设计；RoBERTa 在训练策略、数据规模和动态 mask 等方面做了改进。选做实验可以关注：

- 同一数据集上 BERT 与 RoBERTa 的收敛速度；
- 小样本情况下验证集波动；
- batch size、learning rate、epoch 对结果的影响；
- 是否启用 LoRA 或只微调分类头。

`08_bert_roberta_experiment_planner.py` 用表格化方式生成实验配置，帮助记录不同模型与微调策略的比较结果。

## 七、离线知识 RAG 与在线知识微调的区别

### 7.1 离线知识 RAG

离线 RAG 不改变模型参数，而是在推理时注入外部知识。适合：

- 知识经常更新；
- 数据需要可追溯引用；
- 不希望训练模型；
- 私有知识库和业务文档问答；
- 对事实一致性要求较高的问答系统。

### 7.2 在线知识微调

在线知识微调会把新增知识或任务偏好写入模型参数。适合：

- 任务格式高度稳定；
- 希望模型形成长期行为偏好；
- 响应延迟不允许每次大量检索；
- 有持续收集反馈和评估的机制。

但在线微调风险更高，需要设置数据审核、验证集、回滚策略和安全评测。否则模型可能因为新数据质量差而遗忘原有能力，或者把错误知识固化进参数。

`09_online_finetune_workflow_recorder.py` 以流程记录方式整理“数据进入、质量检查、训练、评估、发布、回滚”的闭环。

## 八、本周作业代码与实验记录

| 文件 | 内容 | 依赖 |
| --- | --- | --- |
| `01_t5_text_to_text_tasks.py` | T5 风格任务统一与样例评估 | 标准库 |
| `02_instruction_tuning_dataset_builder.py` | 指令微调数据构造、校验与划分 | 标准库 |
| `03_peft_method_parameter_accounting.py` | 全量微调、Prompt Tuning、P-Tuning、LoRA 参数量对比 | 标准库 |
| `04_offline_rag_pipeline.py` | 离线 RAG 检索、证据拼接与抽取式回答 | 标准库 |
| `05_hf_text_classification_finetune.py` | HuggingFace 文本分类微调脚本 | 可选 transformers/datasets |
| `06_hf_question_answering_feature_builder.py` | 抽取式问答特征构造与 span 对齐 | 标准库 |
| `07_hf_question_answering_finetune.py` | HuggingFace 问答微调脚本 | 可选 transformers/datasets |
| `08_bert_roberta_experiment_planner.py` | BERT/RoBERTa 微调实验配置规划 | 标准库 |
| `09_online_finetune_workflow_recorder.py` | 在线知识微调流程记录与风险检查 | 标准库 |

本周实验记录关注两类结果：

1. 可直接运行的标准库脚本：记录生成的数据文件、检索结果、参数量对比和工作流审计表；
2. 依赖 HuggingFace 的脚本：在当前未配置环境时记录 dry-run 计划，在具备依赖后可继续执行最小微调样例。
