# 第六周笔记：NLP 任务、词嵌入调优与基础模型应用

## 一、翻译任务：Seq2Seq 与 Attention

### 1.1 任务定义

机器翻译任务的目标是把源语言句子映射为目标语言句子：

```text
source sentence -> target sentence
```

例如：

```text
I like machine learning -> 我 喜欢 机器 学习
```

传统 Seq2Seq 模型通常包含编码器和解码器：

- 编码器读取源语言序列，得到上下文表示；
- 解码器根据上下文表示逐步生成目标语言 token；
- 训练时常用 teacher forcing，用真实前一个目标词作为当前步输入；
- 推理时需要自回归生成，即把模型上一步输出作为下一步输入。

### 1.2 Attention 的作用

早期 Seq2Seq 把整个源句压缩为一个固定长度向量，长句信息容易丢失。Attention 的改进是：解码每个目标词时，动态关注源句中不同位置。

注意力分数可以理解为：

```text
当前解码状态 与 每个源端隐藏状态 的匹配程度
```

这样，生成目标词时模型不必只依赖单个压缩向量，而是可以从源句的全部隐藏状态中加权取信息。第四周已学习过注意力机制，因此本周翻译部分重点放在“如何把注意力用于源端到目标端对齐”。

### 1.3 翻译任务评价

翻译任务常用指标包括：

- token-level accuracy：逐词预测准确率，适合教学小实验；
- BLEU：基于 n-gram 重合度的自动指标；
- 人工评价：语义忠实度、流畅度、术语一致性；
- 错误类型分析：漏译、错译、重复、语序不自然。

本周 `01_translation_seq2seq_attention.py` 使用小型中英平行语料实现一个带注意力的 Seq2Seq 训练流程，并记录不同隐藏维度和学习率下的验证结果。

## 二、问答任务：Question Answering

### 2.1 问答任务类型

问答任务可以分为多种形式：

1. 抽取式问答：从给定文章中抽取答案片段；
2. 生成式问答：模型根据上下文生成自然语言答案；
3. 检索式问答：先检索相关文档，再抽取或生成答案；
4. 多跳问答：需要组合多个句子或文档中的证据。

教学中常见的是抽取式 QA。输入通常包括：

```text
context + question -> answer span
```

模型需要预测答案的起始位置和结束位置。

### 2.2 关键建模问题

QA 任务的难点不只是找到关键词，而是理解问题与上下文之间的语义匹配。例如：

- 问题中的实体和上下文实体是否对应；
- 答案类型是否匹配问题类型，如人物、地点、时间、原因；
- 同一个词在上下文中是否存在多个候选位置；
- 问题是否需要跨句推理。

本周 `02_question_answering_span_baseline.py` 实现了一个可调权重的抽取式 QA 基线：根据问题关键词、答案类型和上下文窗口打分，选择最佳答案 span，并输出 EM/F1 与错误样例。

## 三、自然语言生成任务

### 3.1 NLG 的基本任务

自然语言生成可以覆盖摘要、对话、故事续写、问答生成、数据到文本等任务。其共同点是：模型需要在给定条件下生成一段文本。

生成模型通常需要解决两个层面的问题：

- 概率建模：如何估计下一个 token 的概率；
- 解码策略：如何从概率分布中选择最终输出。

### 3.2 常见解码策略

常见解码策略包括：

- Greedy decoding：每一步选概率最高的 token；
- Beam search：保留多个候选序列，通常更稳定；
- Top-k sampling：只在概率最高的 k 个 token 中采样；
- Top-p sampling：从累计概率达到 p 的候选集合中采样；
- Temperature：调节分布尖锐程度，影响生成多样性。

生成任务不能只看 loss，还要关注重复率、多样性、事实一致性和任务适配性。

本周 `03_nlg_decoding_strategies.py` 使用一个 trigram 语言模型对比 greedy、beam、top-k 和 top-p 解码，记录困惑度、distinct-1/2 和生成样例。

## 四、共指消解任务

### 4.1 任务定义

共指消解的目标是判断文本中哪些 mention 指向同一个实体。例如：

```text
Alice wrote a paper. She submitted it yesterday.
```

其中：

- `Alice` 与 `She` 共指；
- `a paper` 与 `it` 共指。

共指消解结果通常表示为多个 mention cluster，每个 cluster 对应一个实体。

### 4.2 主要难点

共指消解需要处理：

- 代词与先行词匹配；
- 性别、数、语义类型约束；
- 跨句距离；
- 嵌套 mention；
- 事件共指和实体共指的区分。

本周 `04_coreference_resolution_baseline.py` 实现了一个基于 mention 特征、距离惩罚和实体类型兼容性的基线模型，并通过网格搜索调节权重，记录 pairwise F1。

## 五、词嵌入学习：TransE 与 GloVe

### 5.1 TransE

TransE 用于知识图谱嵌入，基本思想是把三元组 `(head, relation, tail)` 建模为向量平移：

```text
h + r ≈ t
```

对于正确三元组，`h + r` 应该接近 `t`；对于负采样三元组，距离应该更远。常用 margin ranking loss：

```text
max(0, margin + positive_score - negative_score)
```

本周 `05_transe_embedding_tuning.py` 构造小型知识图谱，训练 TransE，并对 embedding 维度、margin、学习率进行调优，记录 Hits@1、MRR 和最佳配置。

### 5.2 GloVe

GloVe 的目标是从全局词共现矩阵中学习词向量。它认为词向量点积应拟合词共现次数的对数：

```text
w_i^T w_j + b_i + b_j ≈ log(X_ij)
```

其中权重函数会降低极高频共现的支配作用，并减少低频噪声。

本周 `06_glove_embedding_tuning.py` 根据小语料构建共现矩阵，训练 GloVe 风格词向量，并记录近邻词、loss 曲线和不同窗口大小的结果。

## 六、基础模型应用

### 6.1 序列模型与 Attention

序列注意力模型可以用于分类、翻译和信息抽取。它的关键不是简单“加一层 attention”，而是让模型在输出时给不同 token 分配不同权重，从而增强可解释性和关键证据提取能力。

在本周作业中，情感分析脚本使用注意力池化，让模型自动关注句子中对情感判断更重要的词。

### 6.2 情感分析模型

情感分析是文本分类的典型任务。基本流程包括：

```text
文本 -> 分词 -> 词表 -> embedding -> 编码器 -> 分类头
```

评价指标通常包括 accuracy、precision、recall、F1 和错误样例分析。

本周 `07_sentiment_attention_classifier.py` 训练一个带注意力池化的情感分类器，并对学习率、隐藏维度进行调优。

### 6.3 关系抽取模型

关系抽取任务的目标是判断句子中两个实体之间的语义关系，例如：

```text
Barack Obama was born in Hawaii -> born_in(Obama, Hawaii)
```

模型输入通常包括句子、实体位置、实体类型，输出为关系标签。小型基线可以使用实体标记和上下文池化，大型模型则常结合预训练语言模型。

本周 `08_relation_extraction_toy_model.py` 构造实体标记后的句子，训练一个轻量关系分类模型，并记录 macro-F1 与混淆样例。

### 6.4 大模型任务评估

大模型学习不一定需要在本地训练大模型。阶段二更现实的做法是先建立评估框架：

- 设计 prompt 模板；
- 构造任务样例；
- 保存模型输出；
- 使用规则或人工标准进行评价；
- 记录失败案例和改进方向。

本周 `09_llm_prompt_evaluation_offline.py` 实现一个离线评估框架，不调用外部 API，仅记录 prompt、参考答案、模拟输出、评分和错误类型，方便后续接入真实大模型。

## 七、本周作业代码

```text
第六周作业代码/
├── README.md
├── 01_translation_seq2seq_attention.py
├── 02_question_answering_span_baseline.py
├── 03_nlg_decoding_strategies.py
├── 04_coreference_resolution_baseline.py
├── 05_transe_embedding_tuning.py
├── 06_glove_embedding_tuning.py
├── 07_sentiment_attention_classifier.py
├── 08_relation_extraction_toy_model.py
└── 09_llm_prompt_evaluation_offline.py
```

| 任务内容 | 对应文件 | 实验记录 |
| --- | --- | --- |
| 翻译任务 | `01_translation_seq2seq_attention.py` | Seq2Seq+Attention、验证 token accuracy、调参记录 |
| 问答任务 | `02_question_answering_span_baseline.py` | EM/F1、权重调优、错误样例 |
| 自然语言生成 | `03_nlg_decoding_strategies.py` | 解码策略、perplexity、distinct 指标 |
| 共指消解 | `04_coreference_resolution_baseline.py` | mention cluster、pairwise F1、权重调优 |
| TransE | `05_transe_embedding_tuning.py` | Hits@1、MRR、最佳超参数 |
| GloVe | `06_glove_embedding_tuning.py` | 共现矩阵、loss、近邻词 |
| 情感分析 | `07_sentiment_attention_classifier.py` | attention 分类、accuracy、调参记录 |
| 关系抽取 | `08_relation_extraction_toy_model.py` | 关系分类、macro-F1、错误样例 |
| 大模型 | `09_llm_prompt_evaluation_offline.py` | prompt 评估框架、离线评分、失败类型 |

## 八、阶段二实验记录

阶段二主要涵盖“词嵌入学习”和“基础模型应用”，实验记录了：

1. 数据构造：语料来源、样本数、标签含义；
2. 模型结构：embedding、编码器、打分函数或分类头；
3. 调参范围：学习率、维度、margin、窗口大小、隐藏层大小；
4. 训练指标：loss 曲线、验证准确率、F1、MRR、Hits@K；
5. 最优配置：为什么选择该组参数；
6. 错误样例：模型失败的输入、预测、参考答案和可能原因；
7. 后续改进：数据扩充、预训练模型、负采样策略、解码策略或人工评价。
