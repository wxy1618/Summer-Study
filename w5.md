# 第五周笔记：NLP 基础、词向量、语言模型与阶段一实战

## 自然语言处理导引与词向量

###  NLP 的基本问题

自然语言处理的核心难点在于：人类语言是离散符号系统，但神经网络需要连续数值输入。一个句子在进入模型之前，通常需要经过以下流程：

```text
原始文本 -> 分词 / 子词切分 -> 构建词表 -> token id -> embedding -> 下游模型
```

其中最关键的是表示学习。早期方法常用 one-hot 表示，每个词对应一个极稀疏向量。例如词表大小为 10000，则每个词是一个 10000 维向量，只有一个位置为 1。它的缺点很明显：

- 向量维度随词表线性增长，存储和计算成本高；
- 任意两个不同词的距离都相同，无法表达语义相似性；
- 不能自然表达 “king - man + woman ≈ queen” 这类语义关系。

词向量的目标是把词映射到低维连续空间，让分布相似的词在向量空间中更接近。这里体现了分布式假设：词的意义可以从它出现的上下文中学习。

###  Word2vec 的两种训练目标

Word2vec 的代表性结构包括 CBOW 和 Skip-gram。

CBOW 的输入是上下文词，目标是预测中心词：

```text
context words -> center word
```

Skip-gram 的输入是中心词，目标是预测上下文词：

```text
center word -> context words
```

在小数据或低频词场景中，Skip-gram 往往更适合学习稀有词，因为每个中心词会生成多个训练样本。CBOW 会先聚合上下文，训练速度通常更快，但对低频词的细节表达可能弱一些。

### Softmax 计算瓶颈与 Negative Sampling

若直接用 Softmax 预测整个词表中的下一个词，需要对所有词计算归一化概率。词表很大时，计算成本较高。Negative Sampling 的思想是把多分类问题转化为若干个二分类问题：

- 正样本：真实上下文词；
- 负样本：从噪声分布中采样得到的非上下文词；
- 模型目标：让中心词与正样本更接近，与负样本更远。

常用的负采样分布会对词频做 `3/4` 次幂平滑。这样既保留高频词更常出现的事实，又避免高频词完全支配采样。

### 从 Word2vec 看 Embedding 的意义

Embedding 层本质上是一个可训练查表矩阵。对于词表大小 `V`、向量维度 `D` 的模型，embedding 矩阵形状为：

```text
V x D
```

输入 token id 后，模型直接取出对应行作为该 token 的向量。训练结束后，词向量不只是中间特征，也可以用于相似词检索、聚类、初始化下游模型等任务。

本周 `01_word2vec_skipgram_negative_sampling.py` 使用小语料实现了 Skip-gram + Negative Sampling，用来观察词向量学习、邻近词查询和实验结果保存流程。

## 依存句法分析

### 任务定义

依存句法分析的目标是为句子中的词建立“谁修饰谁”的有向关系。一个依存树通常包含：

- head：支配词；
- dependent：依存词；
- relation label：依存关系类型，如 `nsubj`、`obj`、`amod`；
- root：句子的核心谓词或中心成分。

例如：

```text
I enjoy natural language processing
```

可以分析为：

```text
ROOT -> enjoy
enjoy -> I
enjoy -> processing
processing -> natural
processing -> language
```

### 依存树的特点

一个合格依存树通常满足：

1. 除 ROOT 外，每个词恰好有一个 head；
2. 所有词最终连通到 ROOT；
3. 对多数基础转移系统而言，通常假设树是 projective 的；
4. 边的方向表达句法支配关系，不只是线性相邻关系。

依存句法分析的价值在于，它显式保留了句子的结构信息。对于关系抽取、事件抽取、问答、信息检索等任务，句法结构可以帮助模型区分“表面相近但结构不同”的句子。

### Transition-based Parsing

转移式依存分析将句法树构建看成一个动作序列预测问题。常见状态包括：

- stack：已经读入、正在等待建立依存关系的词；
- buffer：尚未处理的词；
- arcs：已经建立的依存边。

常见动作包括：

- `SHIFT`：从 buffer 取出一个词放入 stack；
- `LEFT_ARC(label)`：建立 `stack[-1] -> stack[-2]`；
- `RIGHT_ARC(label)`：建立 `stack[-2] -> stack[-1]`。

神经网络依存分析器可以把 stack、buffer、arcs 中的局部上下文转为特征，预测下一步动作。训练时通常需要一个 oracle，把金标准依存树转换成动作序列。

本周 `02_dependency_transition_parser.py` 实现了一个小型 arc-standard parser，用于观察状态、动作、oracle 与依存边之间的对应关系。

## 语言模型与 RNN

###  语言模型目标

语言模型的目标是估计一个 token 序列的概率：

```text
P(w1, w2, ..., wn)
```

根据链式法则，可以分解为：

```text
P(w1, ..., wn) = P(w1) * P(w2 | w1) * ... * P(wn | w1, ..., w(n-1))
```

因此，训练语言模型时常用“预测下一个 token”的自监督目标：

```text
输入: w1, w2, ..., w(t-1)
目标: wt
```

这种任务不需要人工标注。文本本身就提供了监督信号，因此语言模型预训练能够利用大量未标注语料。

### RNN 的序列建模思想

RNN 通过隐藏状态保存历史信息：

```text
h_t = f(x_t, h_(t-1))
```

其中 `x_t` 是当前 token 的 embedding，`h_(t-1)` 是之前上下文的压缩表示。最后通过线性层和 Softmax 预测下一个 token。

普通 RNN 的主要问题是长距离依赖较难学习，容易出现梯度消失或梯度爆炸。LSTM 和 GRU 通过门控机制改进这一点：

- 更新门控制保留多少历史信息；
- 重置门控制当前输入如何结合旧状态；
- 门控让模型可以在一定程度上学习“记住”和“遗忘”。

### 困惑度 Perplexity

语言模型常用困惑度评估：

```text
perplexity = exp(cross_entropy)
```

困惑度越低，说明模型对真实文本的平均不确定性越小。直观地说，困惑度可以理解为“模型在每个位置平均还犹豫多少个候选词”。

本周 `03_rnn_language_model.py` 使用 GRU 训练一个小型词级语言模型，记录训练损失、验证困惑度和采样生成结果。

## Transformer 与预训练模型

### Self-attention 的作用

RNN 按时间步顺序处理 token，长距离依赖要经过多个状态传递。Transformer 使用 self-attention，让每个 token 可以直接关注序列中的其他 token。

缩放点积注意力形式为：

```text
Attention(Q, K, V) = softmax(QK^T / sqrt(d_k)) V
```

其中：

- Query 表示当前位置想找什么信息；
- Key 表示其他位置提供什么索引；
- Value 表示其他位置真正提供的内容；
- `sqrt(d_k)` 用来稳定点积尺度，避免 Softmax 过早饱和。

### Multi-head Attention

多头注意力把表示空间拆成多个子空间。不同注意力头可以学习不同关系，例如：

- 局部搭配关系；
- 主谓宾关系；
- 指代关系；
- 位置或格式关系。

多头结果拼接后再经过线性层，使模型能够同时整合多种上下文信号。

### 预训练任务

常见预训练目标包括：

- 自回归语言模型：从左到右预测下一个 token，典型代表是 GPT 类模型；
- Masked Language Modeling：随机遮蔽部分 token，让模型恢复被遮蔽内容，典型代表是 BERT 类模型；
- Encoder-Decoder 预训练：适合翻译、摘要、问答等生成任务；
- 指令微调和偏好对齐：让模型更贴近人类指令和交互需求。

预训练语言模型的核心意义是：先从大规模语料中学习通用语言表示，再通过微调、提示词或检索增强等方式适配具体任务。

本周 `04_transformer_masked_lm.py` 实现了一个小型 Transformer Encoder 的 MLM 任务，用于理解 `[MASK]`、padding mask、位置编码和 token 级预测。

## 大模型与多模态学习

### 大模型相关知识点

大模型可以看作预训练语言模型在参数规模、数据规模、任务覆盖和交互形式上的进一步扩展。学习时需要关注：

- scaling law：模型、数据、算力之间的经验关系；
- instruction tuning：从“续写文本”转向“遵循指令”；
- alignment：通过人类反馈或偏好数据调整模型行为；
- retrieval augmentation：把外部知识库和模型生成结合；
- evaluation：不仅看准确率，还要看鲁棒性、幻觉、安全性和领域适配性。

本周阅读 Datawhale 大模型资料时，重点把大模型知识放回 NLP 主线中理解：Transformer 是结构基础，语言模型目标提供预训练信号，大规模数据和对齐方法让模型具备更强的通用任务接口。

###  多模态学习

多模态学习关注文本、图像、音频、视频等不同模态之间的表示对齐。以图文任务为例，常见思路是：

```text
图像编码器 -> image embedding
文本编码器 -> text embedding
对比学习 -> 让匹配图文更接近，不匹配图文更远
```

多模态模型的关键问题包括：

- 不同模态的信息密度和噪声不同；
- 图像区域与文本短语之间不是天然一一对应；
- 训练数据可能存在弱标注、偏见或配对错误；
- 下游任务既可能要求检索，也可能要求生成和推理。

本周 `07_toy_multimodal_retrieval.py` 用一个轻量图文检索实验模拟对比学习过程，用来理解“共享语义空间”的建模思想。

## fastbook 阶段一实战流程

###  fastbook NLP 章节要点

fastbook 的 NLP deep dive 强调从文本到语言模型、再到文本分类器的迁移流程。核心步骤是：

1. Tokenization：把文本切成词、子词或字符；
2. Numericalization：构建词表并把 token 转为整数；
3. Language Model DataLoader：构造输入序列和右移一位的预测目标；
4. 语言模型训练或微调：学习语料风格；
5. 分类模型训练：复用语言模型 encoder，加上分类 head；
6. 渐进式解冻和判别式学习率：减少对预训练表示的破坏。

这条流程的重点不是“模型一次训练完”，而是先让模型学会目标语料的表达习惯，再转到监督任务。

### fastbook Mid-level Data 章节要点

Mid-level API 关注数据处理的可组合性。它把高层 DataBlock 背后的步骤拆开：

- `Transform`：一个可调用的数据变换对象，可以有 `setup` 和 `decode`；
- `Pipeline`：把多个 transform 串联起来；
- `TfmdLists` / `Datasets`：保存原始样本、变换规则和切分信息；
- `DataLoaders`：把样本组织成 batch，并在 item 或 batch 层应用变换。

学习这部分的收获是：遇到真实项目中“不完全符合标准目录结构”的数据时，可以自己组织数据流，而不是只能套用高层接口。

本周 `05_stage1_nlp_text_classifier.py` 和 `06_midlevel_data_api_demo.py` 分别从“完整 NLP 模型流程”和“中层数据 API 思想”两个角度完成阶段一实践。

## 本周作业代码文件对应关系

```text
第五周作业代码/
├── README.md
├── 01_word2vec_skipgram_negative_sampling.py
├── 02_dependency_transition_parser.py
├── 03_rnn_language_model.py
├── 04_transformer_masked_lm.py
├── 05_stage1_nlp_text_classifier.py
├── 06_midlevel_data_api_demo.py
└── 07_toy_multimodal_retrieval.py
```

对应关系如下：

| 任务内容 | 对应文件 | 主要产出 |
| --- | --- | --- |
| 词向量与 Word2vec | `01_word2vec_skipgram_negative_sampling.py` | 词向量、近邻词、loss 记录 |
| 依存分析 | `02_dependency_transition_parser.py` | oracle 动作序列、依存边 |
| 语言模型与 RNN | `03_rnn_language_model.py` | GRU 语言模型、困惑度、生成样例 |
| Transformer 与预训练 | `04_transformer_masked_lm.py` | MLM 训练、mask 预测准确率 |
| fastbook NLP 阶段一 | `05_stage1_nlp_text_classifier.py` | 文本分类实验、准确率、结果 JSON |
| fastbook Mid-level Data | `06_midlevel_data_api_demo.py` | Transform/Pipeline/数值化/解码演示 |
| 多模态学习 | `07_toy_multimodal_retrieval.py` | 图文对齐、检索 Recall@1 |

## 阶段一实验记录思路

实验不追求大规模数据，强调把 NLP 模型构建的关键环节跑通。记录实验结果时保留了实验可复现所需的上下文。写清楚：

1. 数据来源：脚本内置小语料或小型合成数据；
2. 预处理方式：分词规则、词表大小、padding 策略；
3. 模型结构：embedding、RNN/Transformer/分类头；
4. 训练配置：epoch、batch size、learning rate、随机种子；
5. 评估指标：loss、accuracy、perplexity、Recall@1；
6. 观察结论：模型是否学习到任务规律，错误可能来自哪里。


