# 第四周笔记：大模型、图像生成、PyTorch 进阶与 PaddlePaddle 实践

## 本周学习目标

1. 理解大模型能力随模型规模、数据规模变化的基本规律。
2. 掌握涌现能力、数据规模与 KNN-LM 的核心思想。
3. 理解 VAE、Flow、GAN、Diffusion 等图像生成模型的差异。
4. 完成 Self-attention、Transformer 和生成模型相关课程作业代码。
5. 补全 PyTorch 入门教程中的数据、模型、训练、评估和保存流程。
6. 在不执行环境安装的前提下，完成 PaddlePaddle 初步实践代码。

## 大模型与大数据

### P29：大模型的“顿悟时刻”

#### 1.1 什么是涌现能力

涌现能力（Emergent Ability）通常指一种观察现象：小模型在某项任务上的表现接近随机或几乎为零，而模型规模增加到某个区间后，性能突然明显提升。从曲线外观上看，这种变化像模型在某个时刻突然“学会”了能力，因此课程中形象地称为“顿悟时刻”。

常见讨论对象包括：

- 多步骤算术与逻辑推理；
- 少样本学习和上下文学习；
- 指令理解与跨任务迁移；
- 代码生成和复杂问题求解。

需要注意，模型能力不一定真的在某个精确参数量上突然出现。若评价指标是“整题完全正确才计分”，底层能力的连续提升也可能在最终分数上表现为跳变。因此，判断涌现现象时应同时观察：

- 连续指标与离散指标；
- 不同提示词下的结果；
- 多个模型规模和多次实验；
- 平均性能、方差和错误类型。

#### 1.2 Scaling Law 的基本认识

Scaling Law 描述模型性能与以下因素之间的经验关系：

- 模型参数量；
- 训练数据量；
- 训练计算量；
- 数据质量和任务难度。

模型变大通常能提高表示能力，但“参数更多”并不自动等于“效果更好”。如果数据量、数据质量或训练计算不足，大模型仍可能训练不充分。实际设计中需要在模型、数据和算力之间进行配比。

#### 1.3 对涌现能力的理性判断

学习这部分内容时，应避免两个极端：

- 不能因为出现能力跃升，就认为模型产生了无法解释的“意识”。
- 也不能因为评价指标可能制造跳变，就否认规模扩展带来的真实能力提升。

更稳妥的结论是：大模型确实可能在规模扩大后表现出新的任务能力，但“是否突然出现”与测试方法、指标定义、提示方式和采样误差都有关系。

### P30：到底需要多少数据

#### 数据规模不是唯一因素

训练数据越多，模型通常越容易覆盖更多语言模式和知识，但数据的有效性还取决于：

- 数据是否重复；
- 内容是否准确；
- 是否包含噪声、广告或机器生成污染；
- 领域覆盖是否符合目标任务；
- 训练集与测试集是否存在泄漏；
- 数据分布是否均衡。

因此，“一万亿 token”只是规模描述，并不代表每个 token 都有同等训练价值。

#### 模型规模与数据规模要匹配

若模型很大而数据过少，可能出现：

- 模型记忆训练数据；
- 泛化能力不足；
- 训练计算没有充分利用；
- 验证损失提前停止改善。

若数据很多而模型过小，模型容量可能不足以吸收数据中的复杂规律。工程上应通过验证集表现、训练预算和下游任务效果选择较合适的模型与数据配比。

#### 数据质量与数据治理

大规模数据训练前通常需要：

1. 去重，减少相同样本被反复学习。
2. 清洗，过滤乱码、低质量文本和异常内容。
3. 质量评分，对高质量数据设置更高采样权重。
4. 隐私与版权检查，控制敏感数据风险。
5. 测试集去污染，避免模型提前见过评测答案。

核心认识：数据量决定模型能“看多少”，数据质量决定模型能“学到什么”。

###  P31：另辟蹊径——KNN-LM

#### 基本思想

KNN-LM（k-Nearest Neighbors Language Model）不是重新训练一个更大的语言模型，而是在推理时增加一个外部记忆库。

构建记忆库时，将训练文本中每个上下文的隐藏向量作为 key，将它后面的真实词作为 value：

```text
上下文表示 key  ->  下一个 token value
```

预测时执行以下步骤：

1. 用语言模型把当前上下文编码为向量。
2. 在外部记忆库中寻找最相似的 k 个上下文。
3. 根据距离为这些邻居的 token 分配概率。
4. 将 KNN 概率与原语言模型概率进行插值。

插值形式可以写为：

```text
P_final(w | x) = lambda * P_knn(w | x)
                 + (1 - lambda) * P_lm(w | x)
```

其中 `lambda` 控制“外部记忆”和“模型自身预测”的占比。

#### KNN-LM 的特点

优点：

- 不必重新训练主模型；
- 可以通过替换记忆库快速适应新领域；
- 对低频事实、专有名词和长尾模式可能更有效；
- 记忆库内容可更新。

限制：

- 检索增加推理延迟；
- 大型记忆库占用存储空间；
- 向量索引质量会影响结果；
- 检索到错误或过时内容时会干扰预测。

本周 `01_knn_language_model.py` 使用标准库实现了一个小型 KNN-LM 思想演示，用于观察基础语言模型与邻居概率插值后的预测变化。

### P32：GPT-4 能力的课程阶段性观察

这一集课程发布于 2023 年，应把内容视为当时对 GPT-4 的阶段性观察，而不是对当前所有模型的统一结论。

课程关注的能力包括：

- 更强的指令遵循和复杂任务处理；
- 图像与文字共同输入的多模态能力；
- 在多种专业考试和基准上的性能提升；
- 对长文本、代码和推理任务的改进。

同时，大模型仍存在以下问题：

- 可能生成事实错误或虚构内容；
- 结果受提示词和上下文影响；
- 不能把基准分数等同于真实理解；
- 训练数据、评测污染和安全边界需要持续检查。

### P33：Homework 4——Self-attention

#### Self-attention 的计算

输入序列经过线性变换得到 Query、Key 和 Value：

```text
Q = XW_Q
K = XW_K
V = XW_V
```

缩放点积注意力为：

```text
Attention(Q, K, V) = softmax(QK^T / sqrt(d_k))V
```

含义如下：

- Query 表示当前位置想寻找什么信息；
- Key 表示每个位置可供匹配的特征；
- Value 表示匹配后真正汇总的内容；
- `sqrt(d_k)` 用于控制点积数值范围，改善训练稳定性。

#### Multi-head Attention

多头注意力让模型在不同子空间中并行学习关系。例如，一个头关注相邻词，另一个头关注远距离依赖。各个头的结果拼接后再进行线性映射。

#### 作业代码

`02_self_attention_homework.py` 构造一个合成序列分类任务：标签取决于两个特殊 token 的先后关系。代码包含：

- 词嵌入与位置嵌入；
- `nn.MultiheadAttention`；
- padding mask；
- 训练与验证；
- 最佳模型保存。

这个任务特意设计为依赖顺序，从而说明只有 token 内容还不够，还需要位置编码。

## 图像生成模型

###  常见生成模型总览

生成模型的目标是学习训练数据的分布，并从中采样新样本。课程涉及的常见路线如下：

| 模型 | 核心思路 | 主要优点 | 常见限制 |
| --- | --- | --- | --- |
| Autoregressive | 按顺序逐像素或逐 token 生成 | 概率定义清晰 | 生成速度较慢 |
| VAE | 编码到潜空间，再从潜变量解码 | 潜空间连续、训练稳定 | 图像可能偏平滑 |
| Flow | 可逆变换连接简单分布与数据分布 | 可精确计算似然 | 可逆结构限制较强 |
| GAN | 生成器与判别器对抗训练 | 采样快、图像锐利 | 训练不稳定、可能模式坍塌 |
| Diffusion | 逐步加噪，再学习逐步去噪 | 质量高、训练较稳定 | 采样步骤通常较多 |

### P35：Diffusion Model 原理

#### 前向扩散

前向过程逐步向真实样本 `x_0` 添加高斯噪声：

```text
q(x_t | x_(t-1)) = N(sqrt(1 - beta_t) * x_(t-1), beta_t * I)
```

利用重参数化，可以直接从 `x_0` 得到任意时刻 `x_t`：

```text
x_t = sqrt(alpha_bar_t) * x_0
      + sqrt(1 - alpha_bar_t) * epsilon
```

其中 `epsilon` 是标准高斯噪声。

#### 反向去噪

模型接收带噪样本 `x_t` 和时间步 `t`，预测加入的噪声。常见训练目标是均方误差：

```text
loss = MSE(epsilon_pred, epsilon)
```

生成时从随机高斯噪声开始，反复调用模型去噪，逐渐得到数据样本。

#### 条件生成

如果希望按照文字生成图像，还需要把文字条件送入去噪网络。常见方法包括：

- 文本编码器生成文字表示；
- Cross-attention 将文字信息注入图像特征；
- Classifier-free Guidance 增强生成结果与提示词的一致性。

`05_diffusion_toy_demo.py` 在二维数据上实现前向加噪、噪声预测训练和反向采样，避免下载大数据集，也能完整观察 Diffusion 的基本流程。

### P36：Stable Diffusion、DALL-E 与 Imagen 的共同套路

不同系统的具体结构和训练数据并不相同，但可以抽象出相似模块：

```text
文字提示
  -> 文本编码器
  -> 条件图像生成器
  -> 图像表示
  -> 解码或超分辨率模块
  -> 最终图像
```

#### 文本编码器

文本编码器把自然语言提示词转换为向量，使图像生成器能够使用文字语义。编码器质量会影响模型对物体、属性、关系和风格的理解。

#### 潜空间生成

直接在高分辨率像素空间反复去噪计算量很大。Latent Diffusion 先用自动编码器将图像压缩到潜空间，在较小的潜表示上执行扩散，再解码回图像，因此能显著降低计算开销。

#### 3.3 级联与超分辨率

有些系统先生成较低分辨率图像，再通过一个或多个超分辨率模型逐步放大。这样可以把“语义构图”和“高分辨率细节”分阶段完成。

###  P39：GAN Lecture

GAN 包含两个网络：

- Generator：把随机噪声变成假样本；
- Discriminator：判断输入来自真实数据还是生成器。

二者形成一个对抗目标：

```text
min_G max_D E[log D(x)] + E[log(1 - D(G(z)))]
```

直观理解：判别器不断提高识别假图的能力，生成器不断提高欺骗判别器的能力。理想状态下，生成分布接近真实数据分布，判别器难以区分二者。

训练 GAN 时常见问题：

- 判别器过强，生成器梯度变弱；
- 生成器只产生少量相似样本，即模式坍塌；
- loss 数值不一定与视觉质量同步；
- 学习率、归一化和网络结构对稳定性影响较大。

`06_gan_image_homework.py` 使用程序生成的 16×16 几何图形训练一个轻量 GAN，并把生成结果保存为 PGM 图片网格。

### P40：Homework 5——Transformer

Transformer 通常由以下部分组成：

- token embedding；
- positional encoding；
- encoder self-attention；
- decoder masked self-attention；
- encoder-decoder cross-attention；
- feed-forward network；
- residual connection 与 normalization。

Decoder 中的 causal mask 用来遮住未来 token，保证生成第 `t` 个 token 时不能偷看后面的正确答案。

`04_transformer_homework.py` 实现了一个小型序列反转任务：输入一串 token，模型按相反顺序生成目标序列。代码包含 teacher forcing、causal mask、训练、验证和自回归推理。

###  P41：Homework 6——Generative Model

课程 Homework 6 对应生成模型。实践时不仅应观察 loss，还要关注：

- 生成样本是否清晰；
- 样本是否具有多样性；
- 是否出现模式坍塌；
- 固定噪声下的生成结果是否逐步改善；
- 训练结果是否能够复现。

本周使用 `06_gan_image_homework.py` 完成一个无需下载数据的生成模型作业框架。脚本会固定一组噪声，并在训练结束后输出图像网格，便于比较结果。

## PyTorch 入门教程剩余内容

### Dataset 与 DataLoader

`Dataset` 负责定义单个样本如何读取，通常实现：

```python
def __len__(self):
    ...

def __getitem__(self, index):
    ...
```

`DataLoader` 负责批量读取、打乱顺序和并行加载：

```python
loader = DataLoader(
    dataset,
    batch_size=32,
    shuffle=True,
)
```

### 数据变换

Transform 用来把原始样本转换为模型需要的形式，例如：

- 转为 Tensor；
- 标准化；
- 图像裁剪、翻转和缩放；
- 标签编码。

数据增强只应作用于训练集。验证集和测试集通常只进行确定性的预处理，以保证评价公平。

### 构建模型

自定义模型继承 `nn.Module`，在 `__init__()` 中声明层，在 `forward()` 中定义前向传播：

```python
class Classifier(nn.Module):
    def __init__(self) -> None:
        super().__init__()
        self.linear = nn.Linear(10, 3)

    def forward(self, inputs: torch.Tensor) -> torch.Tensor:
        return self.linear(inputs)
```

### 自动求导

PyTorch 会在前向计算时构建动态计算图，调用 `loss.backward()` 后，根据链式法则计算参数梯度。

推理和验证时使用：

```python
model.eval()
with torch.no_grad():
    predictions = model(inputs)
```

`model.eval()` 会切换 Dropout、BatchNorm 等层的行为；`torch.no_grad()` 则关闭梯度记录并减少内存消耗，二者作用不同。

### 优化循环

标准训练步骤：

1. 将模型设为训练模式。
2. 读取一个 mini-batch。
3. 执行前向传播。
4. 计算损失。
5. 清空旧梯度。
6. 反向传播。
7. 更新参数。

```python
optimizer.zero_grad()
logits = model(features)
loss = loss_fn(logits, labels)
loss.backward()
optimizer.step()
```

### 保存与加载

推荐保存模型的 `state_dict`，而不是直接序列化整个模型对象：

```python
torch.save(model.state_dict(), "model.pt")
model.load_state_dict(torch.load("model.pt"))
```

如果要恢复训练，还应保存 optimizer 状态、epoch 和验证指标等信息。

### 本周综合教程代码

`03_pytorch_complete_tutorial.py` 将教程内容串为一条完整流程：

```text
自定义 Dataset
-> DataLoader
-> 模型定义
-> 训练
-> 验证
-> checkpoint 保存
-> checkpoint 加载
-> 单样本推理
```

## PaddlePaddle 初步实践

### 本周范围说明

按照要求，本周不执行 PaddlePaddle、CUDA、conda 或其他环境安装。只生成实践代码，运行时如果没有安装 PaddlePaddle，脚本会给出说明并正常退出。

### 核心 API 对照

| 功能 | PyTorch | PaddlePaddle |
| --- | --- | --- |
| 模型基类 | `torch.nn.Module` | `paddle.nn.Layer` |
| 数据集 | `torch.utils.data.Dataset` | `paddle.io.Dataset` |
| 数据加载 | `DataLoader` | `paddle.io.DataLoader` |
| 反向传播 | `loss.backward()` | `loss.backward()` |
| 参数更新 | `optimizer.step()` | `optimizer.step()` |
| 清空梯度 | `optimizer.zero_grad()` | `optimizer.clear_grad()` |
| 保存参数 | `torch.save()` | `paddle.save()` |
| 加载参数 | `torch.load()` | `paddle.load()` |

###  实践代码内容

`07_paddle_initial_practice.py` 包含：

- Tensor 创建与形状查看；
- 自动求导；
- 自定义 Dataset 与 DataLoader；
- `paddle.nn.Layer` 模型；
- 训练与验证；
- 参数保存与重新加载；
- 简单推理。

## 代码规范

本周代码按照 PEP 8 的核心建议组织：

- 使用 4 个空格缩进；
- 模块名、函数名和变量名使用 `snake_case`；
- 类名使用 `CapWords`；
- 常量使用 `UPPER_CASE`；
- import 分组并放在文件开头；
- 公开函数提供类型标注与文档字符串；
- 长表达式使用括号自然换行；
- 脚本统一提供 `if __name__ == "__main__":` 入口；
- 注释解释“为什么这样做”，避免只重复代码表面含义。

本周每个 Python 文件的独立注释行数均不低于有效代码行数的 20%。文档字符串未计入该比例，因此实际可读性说明更多。

## 作业代码目录

```text
第四周作业代码/
├── README.md
├── 01_knn_language_model.py
├── 02_self_attention_homework.py
├── 03_pytorch_complete_tutorial.py
├── 04_transformer_homework.py
├── 05_diffusion_toy_demo.py
├── 06_gan_image_homework.py
└── 07_paddle_initial_practice.py
```
