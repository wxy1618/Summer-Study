# 第八周笔记：知识增强、伦理道德、模型分析与 PaddleNLP 阶段四实践

## 一、本周学习目标

第八周的学习内容从前几周的模型训练与微调继续向“模型应用可信性”推进。第七周已经学习了 T5、ChatGPT 原理、指令微调、LoRA、RAG 与 HuggingFace 微调流程；第八周进一步关注四个问题：

1. 如何向语言模型加入外部知识，使模型回答更可靠；
2. 如何评估 NLP/大模型系统的社会伦理风险；
3. 如何分析模型行为并给出可解释证据；
4. 如何通过 PaddleNLP 平台完成语义检索、智能问答和情感分析等预训练模型应用流程。

本周的核心不是单纯追求模型跑通，而是把“知识、数据、模型、评估、风险控制”连接成一个完整实验闭环。

## 二、知识增强：Add Knowledge to Language Model

### 2.1 为什么语言模型需要知识增强

语言模型在预训练中学习到大量参数化知识，但这种知识存在天然限制：

- 训练语料的时间边界决定模型可能不知道新事实；
- 模型参数中存储的知识难以定位来源；
- 相似实体、罕见事实和长尾知识容易混淆；
- 预训练知识无法直接覆盖课程资料、企业文档、私有数据库等本地知识；
- 模型可能生成流畅但无依据的回答，形成幻觉问题。

因此，知识增强的目的不是简单“让模型更大”，而是让模型能够以更可追溯、更可更新、更可控的方式使用知识。

### 2.2 参数化知识与非参数化知识

语言模型中的知识可以粗略分为两类：

1. 参数化知识：知识被压缩在模型权重中，例如预训练和继续训练后形成的事实记忆；
2. 非参数化知识：知识保存在外部文档、知识库、向量索引或数据库中，推理时通过检索调用。

参数化知识的优点是推理速度快、无需额外检索；缺点是更新成本高、来源不透明、容易遗忘。非参数化知识的优点是可更新、可引用、可审计；缺点是依赖检索质量、上下文窗口和 prompt 组织能力。

第七周学习的 RAG 就是典型的非参数化知识增强方法。它把回答过程拆成：

```text
问题 -> 检索证据 -> 拼接上下文 -> 生成回答 -> 引用来源
```

### 2.3 常见知识增强方法

知识增强可以发生在不同阶段：

- 预训练阶段：加入领域语料、知识图谱文本化语料或结构化事实；
- 微调阶段：使用领域问答、指令数据或纠错数据让模型学习任务知识；
- 推理阶段：使用 RAG、数据库查询、工具调用或搜索引擎注入外部证据；
- 结构层面：把知识图谱实体、关系、路径信息编码进模型输入或模型结构；
- 评估阶段：用事实一致性、证据覆盖率和引用准确率衡量知识使用是否可靠。

在实际应用中，RAG 与微调不是互相替代，而是互补关系。RAG 更适合动态知识、可追溯问答和文档型任务；微调更适合稳定任务格式、长期行为偏好和领域表达方式。

### 2.4 知识增强的评价维度

知识增强系统不能只看答案是否流畅，还应关注：

- Recall@K：检索阶段是否召回包含答案的证据；
- MRR：正确证据是否排在靠前位置；
- Evidence Coverage：回答是否覆盖关键证据；
- Faithfulness：回答是否严格基于证据；
- Citation Accuracy：引用来源是否真实支持回答；
- Robustness：当检索证据不足时，模型是否能表达不确定性。

本周 `01_knowledge_augmentation_concept_map.py` 和 `02_paddlenlp_semantic_search_dryrun.py` 分别从概念图和语义检索实验角度整理知识增强流程。

## 三、伦理道德：Social & Ethical Consideration

### 3.1 NLP 系统的伦理风险

NLP/大模型系统并不是中性的工具。它们的训练数据、标注过程、部署场景和用户反馈都会影响社会结果。常见风险包括：

- 偏见与歧视：模型可能延续数据中的性别、地域、职业、年龄或群体偏见；
- 隐私泄露：训练数据或用户输入中可能包含敏感信息；
- 虚假信息：模型可以低成本生成看似可信的错误内容；
- 版权与数据来源：语料采集和模型输出可能涉及版权争议；
- 安全滥用：模型可能被用于钓鱼、诈骗、仇恨表达或自动化攻击；
- 责任归属：模型错误造成影响时，需要明确开发者、部署者和使用者责任。

伦理问题不应只放在项目最后检查，而应贯穿数据收集、训练、评估、上线和监控全过程。

### 3.2 数据治理与模型治理

较成熟的治理方式包括：

- 数据说明书：记录数据来源、采集方式、标注规则、适用范围和限制；
- 模型卡：记录模型用途、训练数据、评价指标、已知风险和不适用场景；
- 偏见测试：构造敏感属性对照样本，观察模型是否出现系统性差异；
- 隐私保护：去标识化、最小化收集、权限控制和日志脱敏；
- 人工审核：对高风险领域引入人工复核；
- 回滚机制：当上线模型出现质量或安全退化时可以恢复旧版本。

本周 `05_ethics_and_risk_audit.py` 会把这些要求转成一个可运行的风险审计表，用于记录数据、模型和部署风险。

### 3.3 伦理评价不是“附加题”

在研究生阶段做模型实验时，伦理评价至少应回答以下问题：

1. 数据是否有明确来源，是否包含敏感信息；
2. 模型是否可能对某些群体产生不公平影响；
3. 评价集是否覆盖正常、边界和风险样本；
4. 输出是否需要引用来源或置信度；
5. 用户是否知道系统能力边界；
6. 如果模型产生错误结果，是否有纠错和回滚流程。

这些问题看似不直接提升 accuracy，但决定模型能否被负责地应用。

## 四、模型分析与可解释性

### 4.1 为什么需要模型分析

模型分析的目的不是给模型“找一个好看的解释”，而是理解模型在什么条件下有效、为什么失败、错误是否可预测。常见分析对象包括：

- 错误样例：模型在哪类输入上失败；
- 数据分布：训练集与测试集是否存在分布差异；
- 表示空间：不同类别或实体在向量空间中是否可分；
- 注意力分布：模型是否关注与任务相关的 token；
- 归因结果：哪些词、片段或特征对输出影响最大；
- 反事实样本：当输入中关键属性变化时，模型输出是否合理变化。

### 4.2 可解释性方法

常见方法可以分为几类：

- 梯度/显著性方法：根据梯度或扰动衡量输入 token 的重要性；
- 删除/遮盖实验：逐步移除关键词，观察预测置信度变化；
- 注意力可视化：查看模型不同层或不同头的注意力模式；
- 探针任务：用简单分类器分析隐藏表示是否编码某类信息；
- 反事实解释：构造最小输入变化，观察输出是否发生预期改变；
- 错误归因：把错误分为检索失败、理解失败、标签噪声、领域迁移失败等。

需要注意的是，解释应区分“看起来合理”和“确实忠实”。例如 attention 权重有时能提供线索，但不一定等同于模型真实决策因果。

### 4.3 本周解释性实验

`06_model_explainability_analysis.py` 使用标准库实现一个轻量解释实验。它会对情感分析样本做：

- 词级贡献统计；
- 删除实验；
- 反事实替换；
- 错误类型归纳；
- JSON 和 Markdown 结果记录。

该实验不是为了替代深度模型解释工具，而是训练“把解释变成可检验过程”的习惯。

## 五、NLP 未来

### 5.1 从任务模型到通用系统

早期 NLP 多以单任务模型为主，例如分类、序列标注、翻译、问答。随着预训练模型和大语言模型发展，NLP 系统逐渐转向：

- 多任务统一建模；
- 指令驱动交互；
- RAG 与工具调用；
- 多模态理解与生成；
- Agent 式任务分解；
- 小模型蒸馏与高效部署；
- 人类反馈和持续学习闭环。

这意味着未来 NLP 研究不仅要懂模型，也要懂数据、交互、评估、安全和工程部署。

### 5.2 未来值得关注的方向

结合前几周课程内容，我认为后续学习可以重点关注：

- 可靠 RAG：检索、重排、引用、事实一致性评估；
- 高效微调：LoRA、Prefix/Prompt Tuning、量化训练；
- 多模态文档理解：OCR、版面结构、图文问答；
- 可解释与可控生成：让模型输出更可审计；
- 低资源与领域迁移：小样本场景下如何稳定泛化；
- 隐私保护与合规：数据治理、模型卡、风险监控；
- 国产深度学习平台：PaddlePaddle/PaddleNLP 等生态实践。

## 六、PaddleNLP 阶段四实践

### 6.1 版本与路径说明

本周任务中给出的 PaddleNLP 链接属于 `applications/...` 路径。核对当前官方仓库后可以看到，PaddleNLP 的示例目录在新版结构中存在迁移，例如语义索引和情感分析示例更多位于 `slm/examples/...` 路径下。为了避免依赖某一个旧路径，本周代码采用“平台流程 dry-run + 标准库可运行实验”的方式：

- 默认不安装 PaddlePaddle/PaddleNLP；
- 默认不下载预训练模型；
- 默认不读取外部数据集；
- 通过标准库模拟数据处理、检索、评价和报告记录；
- 若用户本地已有 PaddleNLP 环境，可显式添加参数进入真实调用路径。

### 6.2 语义检索系统

语义检索系统一般包括：

1. 构建查询和文档集合；
2. 用 tokenizer 和预训练模型编码 query/document；
3. 计算向量相似度；
4. 使用 Recall@K、MRR、nDCG 等指标评估检索质量；
5. 记录错误样例，例如正确文档未召回、同义表达失败、领域词不匹配。

本周 `02_paddlenlp_semantic_search_dryrun.py` 默认使用 TF-IDF 做离线检索基线，同时保留 PaddleNLP 调用说明。这样可以在未配置环境时仍然完成实验记录。

### 6.3 智能问答系统 / 文档问答

智能问答系统比普通文本分类更依赖证据链。对于文档问答或 DocVQA，还需要处理：

- OCR 文本；
- 文档布局；
- 问题意图；
- 答案所在区域；
- 文档图像与文本的对齐关系；
- 证据引用和置信度。

本周 `03_paddlenlp_doc_vqa_workflow.py` 用结构化 OCR token、bounding box 和问题样例模拟 DocVQA 数据流，并输出答案、证据 token 与置信度记录。

### 6.4 情感分析

情感分析是 PaddleNLP 中较典型的预训练模型微调任务。完整流程包括：

1. 准备文本与标签；
2. 划分训练、验证和测试集；
3. 加载 tokenizer 和预训练分类模型；
4. 训练并记录 loss、accuracy、F1；
5. 分析错误样例；
6. 保存模型和实验指标。

本周 `04_paddlenlp_sentiment_analysis_dryrun.py` 默认使用标准库词典基线完成情感分类和指标记录，同时保留 PaddleNLP 微调入口说明。

## 七、本周作业代码列表

| 文件 | 内容 | 依赖 |
| --- | --- | --- |
| `01_knowledge_augmentation_concept_map.py` | 知识增强概念图、方法对比与评价指标记录 | 标准库 |
| `02_paddlenlp_semantic_search_dryrun.py` | 语义检索系统 dry-run 与 TF-IDF 检索基线 | 标准库；可选 PaddleNLP |
| `03_paddlenlp_doc_vqa_workflow.py` | 智能问答/DocVQA 数据流、证据定位与答案记录 | 标准库；可选 PaddleNLP Taskflow |
| `04_paddlenlp_sentiment_analysis_dryrun.py` | 情感分析 dry-run、指标计算与 PaddleNLP 微调流程说明 | 标准库；可选 PaddleNLP |
| `05_ethics_and_risk_audit.py` | 数据、模型、部署伦理风险审计表 | 标准库 |
| `06_model_explainability_analysis.py` | 情感分析解释性、删除实验与反事实分析 | 标准库 |
| `07_stage4_experiment_report_builder.py` | 汇总阶段四实验结果，生成 JSON/Markdown 报告 | 标准库 |

## 八、本周总结

第八周的内容提醒我，模型能力并不只由参数量和训练指标决定。知识增强决定模型能否接入新知识，伦理治理决定模型能否被负责地使用，可解释性决定模型失败时能否被分析和改进，平台化实践则决定研究方法能否落到可复现的工程流程中。

在 PaddleNLP 阶段四实践中，本周代码不追求直接配置环境，而是把语义检索、文档问答和情感分析拆成清晰的数据结构、模型调用边界、评价指标和实验报告。后续如果进入已有 PaddleNLP 环境，可以在这些 dry-run 脚本基础上替换真实 tokenizer、预训练模型和训练循环。
